env:
  # Frontend Configuration
  NODE_ENV: staging
  # Internal URL for server-side calls (Next.js Proxy -> Backend Service)
  INTERNAL_API_URL: http://manufacturing-car-manual-rag-web-app

serviceAccount:
  enabled: false

ingress:
  enabled: true
  hosts:
    - manufacturing-car-manual-rag-frontend.industrysolutions.staging.corp.mongodb.com

mesh:
  enabled: true

resources:
  limits:
    cpu: 1000m
    memory: 1024Mi
  requests:
    cpu: 250m
    memory: 256Mi
